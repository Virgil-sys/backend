{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python3 manage.py collectstatic --noinput && python3 manage.py migrate && python3 -m gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
