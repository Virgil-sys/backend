{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python backend/manage.py collectstatic --noinput && python backend/manage.py migrate && gunicorn backend.wsgi --chdir backend --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
